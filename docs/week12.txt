mmap

- msync - face propagarea manuala a schimbarilor din memorie pe disc
- MAP_PRIVATE + msync still does not do the job
- kernel handles writing to this of the dirty pages

map - persistent between files
  - not persistent - ipc

non-persistent map - anonymous with forking
  - with names
    - kept alive until
      - closing the kernel
      - all processes will unlink

shm_open
ftruncate
mmap/munmap
fstat/fchmod
open/close
shm_unlink

long PageSize = sysconf(_SC_PAGE_SIZE);

- 3 tipuri de exercitii
